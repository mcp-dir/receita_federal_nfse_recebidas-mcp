# Ferramentas

Receita Federal NFS-e: Notas Recebidas expõe 1 ferramenta (todas somente leitura).

### 1. `receita_federal_nfse_recebidas_consultar`
**Input**: `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `login_cpf` (opcional), `login_cnpj` (opcional), `login_senha` (opcional), `data_inicial` (opcional), `data_final` (opcional), `pagina` (opcional)

Receita Federal NFS-e: Notas Recebidas, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Receita Federal NFS-e: Notas Recebidas
```
